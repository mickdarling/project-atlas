# Harvested facts — regenerate any time with `npm run scan`.
data/inventory.json
data/scan-progress.json
data/*.tmp

.DS_Store
node_modules/
data/prefs.json

# Artifacts stay LOCAL. The generator is public; what it generates about
# YOUR portfolio never leaves your machine.
data/verdicts.json
data/identity.json

# Dollhouse MCP session cache — tool droppings, not project files.
.dollhousemcp/
